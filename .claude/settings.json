{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "cd '/c/Users/dessa/OneDrive/Área de Trabalho/PROJETOCLAUDECODE' && git add -A && staged=$(git diff --cached --name-only) && if [ -n \"$staged\" ]; then ts=$(date '+%Y-%m-%d %H:%M:%S'); git commit -m \"auto-update: $ts\"; fi; git push 2>/dev/null || true",
            "statusMessage": "Syncing to GitHub...",
            "async": true,
            "timeout": 30
          }
        ]
      }
    ]
  }
}
